<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
         stopOnError="false"
         stopOnFailure="false">

    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Integration">
            <directory>tests/Integration</directory>
        </testsuite>
    </testsuites>

    <php>
        <env name="APP_ENV"    value="test" force="true"/>
        <env name="APP_DEBUG"  value="0"    force="true"/>
        <env name="KERNEL_CLASS" value="App\Kernel" force="true"/>

        <!-- PUT YOUR DATABASE_URL DIRECTLY HERE — bypasses .env.test completely below is for local development -->
        <!-- <env name="DATABASE_URL"
             value="mysql://root:@127.0.0.1:3306/paysera?serverVersion=8.0&amp;charset=utf8mb4"
             force="true"/>

        <env name="REDIS_URL"
             value="redis://127.0.0.1:6379"
             force="true"/> -->


        <!-- PUT YOUR DATABASE_URL DIRECTLY HERE — bypasses .env.test completely below is for docker development -->
        <env name="DATABASE_URL"
             value="mysql://root:rootsecret@db:3306/paysera?serverVersion=8.0&amp;charset=utf8mb4"
             force="true"/>

        <env name="REDIS_URL"
             value="redis://redis:6379"
             force="true"/>

         <!-- THIS is what was missing — routing needs a base URL in test/CLI -->
        <env name="DEFAULT_URI"
             value="http://localhost:8080"
             force="true"/>     
    </php>

</phpunit>